# SafeLoop Final Round 2 report

Committed submission artifacts:

- `5ChangLinhNguLam_Final_R2.docx`: editable report for the official packet.
- `5ChangLinhNguLam_Final_R2_source.html`: reproducible source used to generate the DOCX.

Before submission, the team lead must fill the Video URL in both its locations and the Evidence Folder URL on the cover page. The report intentionally declares the CarSky mode as `MODEL_OUTPUT_REPLAY`, not live image inference.

The Evidence Folder/ZIP, copied model artifacts, render checks, ODT intermediate and pre-final document are local upload artifacts. They are intentionally excluded from Git to avoid duplicating large binaries already represented by source artifacts and SHA-256 manifests.
